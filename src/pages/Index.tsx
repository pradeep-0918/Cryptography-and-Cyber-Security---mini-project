import Hero from "@/components/Hero";
import EncryptionVisual from "@/components/EncryptionVisual";

const Index = () => {
  return (
    <div className="min-h-screen">
      <Hero />
      <EncryptionVisual />
    </div>
  );
};

export default Index;
